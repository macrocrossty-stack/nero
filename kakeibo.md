# 家計簿システム

家庭の収支を Notion + ダッシュボード + 自動計上Routine で継続管理する仕組み。2026-08-05 構築。

## 全体像

```
Notion「収支レコード」DB  ←── 手入力(変動費・収入) + Routine(固定費を毎月自動計上)
        │
        ▼ (閲覧時にSQLクエリで直接読み取り)
HTMLダッシュボード (Artifact) ── 月別サマリー / カテゴリ別円グラフ / 推移 / 記録一覧
```

## 構成要素

### Notion

- **💰 家計簿(ホーム)**: https://app.notion.com/p/3b3a67f7d44a817a907fe17ff439eb98
- **収支レコード** (DB): https://app.notion.com/p/234f5b9ec3e74664b12808aa9f874ff5
  - データソースID: `collection://c9691ce6-8691-43a7-916b-8f977bc24af7`
  - プロパティ: 項目 / 日付 / 金額(¥) / 種別(支出・収入) / カテゴリ(17種) / 固定費(チェック) / メモ
- **固定費マスタ** (DB): https://app.notion.com/p/7908268c624f4d8194a9a8b4bb0624f5
  - データソースID: `collection://ed0e90bf-0692-45db-8ef1-5d256c7275e4`
  - プロパティ: 項目 / 金額(¥) / カテゴリ / 支払日(日) / 有効(チェック) / メモ
  - 金額を実際の値にして「有効」にチェックを入れた行だけが自動計上の対象

### ダッシュボード

- 公開URL: https://claude.ai/code/artifact/bb74775f-a47f-4ed7-9284-9742070f4fd7
- ソース: [`kakeibo-dashboard.html`](./kakeibo-dashboard.html)(このファイルを編集して同じパスで再公開すると同URLで更新される)
- Artifactの `mcp` capability で閲覧者のNotionコネクタを使い、`notion-query-data-sources`(SQLモード)を直接呼んで最新データを表示する。データはページに埋め込まれない。
- 内容: 月ナビ付きサマリー(収入/支出/収支/固定費・変動費) / カテゴリ別ドーナツ+内訳バー / 直近6ヶ月の収入・支出グラフ / 固定費マスタ一覧 / 当月の記録テーブル
- カテゴリ色は固定マッピング(色覚多様性・コントラスト検証済みパレット)

### Routine(自動計上)

- 名前: 「家計簿: 毎月の固定費自動計上」(trigger id: `trig_01JToTjacXb6GN2cjQ2aDaog`)
- スケジュール: 毎月1日 01:00 UTC(= 10:00 JST)
- 動作: 固定費マスタの「有効」な行を、当月の収支レコードとして作成(種別=支出、固定費=チェック、日付=当月の支払日)。同名項目が当月に既にあればスキップ(重複防止)。作成のみで更新・削除はしない。

## 運用ルール(続けるコツ)

- 変動費(食費・日用品など)は **週1回、合計でまとめて** 収支レコードに入れる
- 収入は入金日に1行
- 固定費は入力不要(Routineが計上)。金額が変わったら固定費マスタを直す
- 月の振り返りはダッシュボードで
